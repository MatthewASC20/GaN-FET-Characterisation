"""PyQt6 front-end, built in parallel with ``gan_fet.ui`` (tkinter).

Launched with ``gan-fet --qt``, with or without ``--simulate``. The Tk
interface remains the default bench UI until the cutover; when the Qt
front-end has been validated at the bench the default flips and
``gan_fet.ui`` is retired.

PyQt6 is GPLv3-licensed; installing the ``[qt]`` extra places the combined
work under GPLv3.
"""
